<div class="input-group col-12 mb-5">
  <span style="font-size: x-large;">類別* : </span>
  <select name="category" class="custom-select ml-3" style="width: 1em; height:3em; border-color:black" required>
    <option value="" disabled selected>選擇支出類別</option>
    {{#each categoryList}}
    <option value="{{this.id}}">{{this.name}}</option>
    {{/each}}
  </select>
</div>